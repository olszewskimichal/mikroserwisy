spring:
  application:
    name: edge-service
  cloud:
    config:
      uri: http://localhost:8888
logging:
  path: F:/kibana/logi/
  file: F:/kibana/logi/edge_service.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } [%t] %-40.40logger{39} : %m%n%wEx"
server:
  port: 9999
